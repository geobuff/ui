export interface Avatar {
  id: number;
  type: string;
  countryCode: string;
  flagUrl: string;
  name: string;
  description: string;
  primaryImageUrl: string;
  secondaryImageUrl: string;
  gridPlacement: number;
}
