name: dev-cron-create-daily-trivia
on:
  schedule:
    - cron: '*/5 * * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: post /api/daily-trivia
        run: curl -X POST https://geobuff-dev.ts.r.appspot.com/api/daily-trivia
